---
category: Pages
---

A workspace settings screen: a page title, a success message, text and select
inputs, and notification toggles. Use as a settings-form template. Composed from
`PageTitle`, `Message`, `Input`, `Select`, `Field`, `Toggle`, and `Button`. Takes
no props — render `<Settings />`.
