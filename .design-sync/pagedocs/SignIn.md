---
category: Pages
---

A centered sign-in screen: social providers (Google, GitHub, Microsoft), an "or"
divider, and an email form. Use as an authentication entry template. Composed
from `SocialSignInButton`, `Form`, `Input`, and `Button`. Takes no props —
render `<SignIn />`.
